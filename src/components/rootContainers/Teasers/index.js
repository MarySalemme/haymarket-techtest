import React, { Component } from 'react';

/**
 * <NotFound /> component.
 */
class Teasers extends Component {
  /**
   * Renders the Page Not Found.
   *
   * @return {JSX} - page not found markup.
   */
  render() : React.Element<*> {
    return <h3>teasers go here</h3>;
  }
}

export default Teasers;
